GENERAL   

* do without SK_UNKNOWN (post 9 dec 2006 by smcpeak on oink-devel)

* elsa merge plan:
  - gnu_attribute clever idea in gnu.ast (change to list list?)
  - array index designators default in gnu.ast

* check whether IDeclarator.kAndR_params should be nullable
   (kandr.ast)

* check enum support in astgen, especially wrt DeclaratorContext
  for which astgen generates toXml and fromXml functions
  might move DeclaratorContext back into the cc_tcheck.ast

* SubscriptDesignator.idx_computed2 is INT_MIN

* update dependency graph in elsa/gendoc, include ocaml modules, check
   for all modules

* attributes in elsa/in/gnu/attr02.c seem incomplete

Questions to Scott McPeak:

* first attribute in elsa/in/gnu/cil/regparam0.i does not appear in
   the ast??

