// EEb.gr.in   -*- c++ -*-
// a simple ambiguous arithmetic grammar

// E -> E + E | b

terminals {
  p        // plus
  b
}

nonterm E {
  merge(L,R) [ cout << "merging E\n"; return ++count; ]
  dup(v)     [ return ++count; ]
  del(v)     []

  -> a:E p b:E ;
  -> b         ; 
}
